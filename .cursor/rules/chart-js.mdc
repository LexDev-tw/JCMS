---
description: 全專案圖表一律以 Chart.js 生成（Neo-Swiss 色票與 plugin 慣例）
alwaysApply: true
---

# 圖表 — Chart.js 強制

## 原則

- **所有圖表**使用 Chart.js 4.x（`JCMS.html` CDN），輸出於 `<canvas>`。
- **禁止** HTML/CSS/SVG 手刻圖表；**禁止**未明示需求下另引 ECharts、D3、Recharts 等。

## 實作

| 情境 | 做法 |
|------|------|
| 標準柱／折線／堆疊 | Chart.js 內建 `type` + dataset |
| 棒棒糖等特殊造型 | 透明 bar 定位 + **自訂 plugin** 手繪 |
| Vue 主應用 | composable（`use-*-chart.js`） |
| React 子應用 | `useChart` hook + `useEffect` 生命週期 |

## 視覺（Neo-Swiss）

- 色票：`#111111`、`#666666`、`#F05A28`、`#EAEAEA`；關閉或極簡化預設 legend／grid。
- 數字：`font-mono tabular-nums`；金額千分位；參考線標籤靠右、在線上方。

## 參考檔案

- `public/apps/case-stats-app.jsx` — 案件統計
- `public/js/jcms/composables/use-payscale-chart.js` — 俸表棒棒糖 + 金額參考線

完整規範見專案根目錄 `.cursorrules`「圖表（Chart.js 強制）」一節。
